---
product: dd360-ds
title: React MultiRange Slider component
components: MultiRangeSlider
---

# <span name="floating-nav">MultiRangeSlider</span>

    An interactive slider that is used to allow users to select a range of values in a given range. It can be used for a variety of purposes on a website, such as filtering results, setting prices or choosing dates.


### <span name="floating-nav">Import</span>

<WindowEditor codeString="import { MultiRangeSlider } from 'dd360-ds'" />
<WindowEditor codeString="import MultiRangeSlider from 'dd360-ds/MultiRangeSlider'" />

### <span name="floating-nav">Usage</span>

To use MultiRangeSlider you are required to define the following props:

- min: defines the minimum value of the slider.
- max: Defines the maximum value of the slider.
- onChange: Callback function that will be executed every time the slider value changes. It receives an IRangeSlider object containing the current values of the selected range.

<ContainerComponentDoc className="gap-y-2">
    <MultiRangeSlider min="0" max="100" onChange={() => {}} />
</ContainerComponentDoc>

<WindowEditor codeString="import { MultiRangeSlider } from 'dd360-ds'

<MultiRangeSlider min='0' max='100' onChange={() => {}} />" />

#### <span name="floating-nav">MultiRangeSlider barColor</span>

Defines the color of the slider bar.
The default value is #1E40AF.

<ContainerComponentDoc className="gap-y-2">
    <MultiRangeSlider barColor="#ff0000" min="0" max="100" onChange={() => {}} />
</ContainerComponentDoc>

<WindowEditor codeString="import { MultiRangeSlider } from 'dd360-ds'

<MultiRangeSlider barColor='#ff0000' min='0' max='100' onChange={() => {}} />" />

#### <span name="floating-nav">MultiRangeSlider size</span>

Defines the size of the button that selects the ranges. Possible values are "small", "medium", and "large". Default is "small".

<ContainerComponentDoc className="gap-y-2">
    <Text>Small</Text>
    <MultiRangeSlider className="mb-3" size='small' min='0' max='100' onChange={() => {}} />
    <Text>Medium</Text>
    <MultiRangeSlider className="mb-3" size='medium' min='0' max='100' onChange={() => {}} />
    <Text>Large</Text>
    <MultiRangeSlider className="mb-3" size='large' min='0' max='100' onChange={() => {}} className="mt-1" /> 
</ContainerComponentDoc>

<WindowEditor codeString="import { MultiRangeSlider } from 'dd360-ds'

<MultiRangeSlider size='small' min='0' max='100' onChange={() => {}} />
<MultiRangeSlider size='medium' min='0' max='100' onChange={() => {}} />
<MultiRangeSlider size='large' min='0' max='100' onChange={() => {}} />" />


#### <span name="floating-nav">MultiRangeSlider sizeBar</span>

Defines the size of the slider bar. Possible values are "small", "medium", and "large". Default is "small".

<ContainerComponentDoc className="gap-y-2">
    <Text>Small</Text>
    <MultiRangeSlider className="mb-3" sizeBar='small' min='0' max='100' onChange={() => {}} />
    <Text>Medium</Text>
    <MultiRangeSlider className="mb-3" sizeBar='medium' min='0' max='100' onChange={() => {}} />
    <Text>Large</Text>
    <MultiRangeSlider className="mb-3" sizeBar='large' min='0' max='100' onChange={() => {}} /> 
</ContainerComponentDoc>

<WindowEditor codeString="import { MultiRangeSlider } from 'dd360-ds'

<MultiRangeSlider sizeBar='small' min='0' max='100' onChange={() => {}} />
<MultiRangeSlider sizeBar='medium' min='0' max='100' onChange={() => {}} />
<MultiRangeSlider sizeBar='large' min='0' max='100' onChange={() => {}} />" />

### <span name="floating-nav">API Reference</span>

<CustomTableDocs
    dataTable={[
        { title: 'min', default: null, types: ['number'], required: true },
        { title: 'max', default: null, types: ['number'], required: true },
        { title: 'onChange', default: null, types: ['(range: IRangeSlider) => void'], required: true },
        { title: 'initMinValue', default: null, types: ['number'] },
        { title: 'initMaxValue', default: null, types: ['number'] },
        { title: 'minValDisabled', default: 'false', types: ['boolean'] },
        { title: 'maxValDisabled', default: 'false', types: ['boolean'] },
        { title: 'fireReset', default: 'false', types: ['boolean'] },
        { title: 'size', default: "small", types: ['string'] },
        { title: 'sizeBar', default: "small", types: ['string'] },
        { title: 'barColor', default: '#1E40AF', types: ['string'] },
        { title: 'className', default: null, types: ['string'] }
    ]}
/>
