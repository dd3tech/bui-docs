---
product: dd360-ds
title: React AsideModal component
components: AsideModal
---

<br id='top' />
<br />

<Label>Modals</Label>

# <HeaderDocCustom title="AsideModal" pathUrl="modals-asidemodal--aside-modal" />


The AsideModal component in React provides a convenient way to display important content or actions in a dialog that appears next to your main application
content. By using a slide-out or slide-in effect, the AsideModal can draw the user's attention to critical information or provide additional context for an
action without disrupting their workflow. With the ability to fully customize the content and appearance of your AsideModal, you can create a seamless and
intuitive user experience that enhances the usability and effectiveness of your web application.

<br />

##### <span name="floating-nav">Imports</span>

The first alternative is recommended since they can reduce the application bundle.

<WindowEditor codeString="import AsideModal from 'dd360-ds/AsideModal'" />

<WindowEditor codeString="import { AsideModal } from 'dd360-ds'" />

##### <span name="floating-nav">Usage</span>

Use the AsideModal component as shown below:

<Playground>
    <AsideModalCustom />
</Playground>

The code snippet below shows how to use the AsideModal component:

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    title="Component Aside Modal" 
    open
    onClose={() => function('function to close the aside modal')} 
/>
`}/>

<br />

##### <span name="floating-nav">TitleVariant</span>

With the prop <TagDocs text='titleVariant' /> you can customize the size of the title

<Playground>
    <AsideModalCustom haveVariants={true} />
</Playground>

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    variant='h4' 
    title="Component Aside Modal" 
    open
    onClose={() => function('function to close the aside modal')} 
/>
`}/>

<br />

##### <span name="floating-nav">Position</span>

With the prop <TagDocs text='position' /> you can choose on which side of the screen to open the modal

<br />
##### Position Right

<Playground>
    <AsideModalCustom positionAsideModal="right" />
</Playground>

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    position='right' 
    title="Component Aside Modal" 
    open
    onClose={() => function('function to close the aside modal')}
/>
`}/>

##### Position Left

<Playground>
    <AsideModalCustom positionAsideModal="left" />
</Playground>

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    position='left' 
    title="Component Aside Modal" 
    open
    onClose={() => function('function to close the aside modal')}
/>
`}/>

<br />

##### <span name="floating-nav">DisableEscapeKeyDown</span>

With the prop <TagDocs text='disableEscapeKeyDown' /> you can disable the closing of the modal with the esc key

<Playground>
    <AsideModalCustom propsCustom={{ disableEscapeKeyDown: true }} />
</Playground>

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    disableEscapeKeyDown 
    title="Component Aside Modal" 
    open
    onClose={() => function('function to close the aside modal')}
/>
`}/>

<br />

##### <span name="floating-nav">isStickyTitle</span>

With the prop <TagDocs text='IsStickyTitle' /> you can make the title sticky

<Playground>
    <AsideModalCustom title="Component Aside Modal" isStickyTitle>
        <div style={{ padding: '20px', height: '1200px' }}>
            <p>Content</p>
        </div>
    </AsideModalCustom>
</Playground>

<WindowEditor
  codeString={`import AsideModal from 'dd360-ds/AsideModal'

<AsideModal 
    title="Component Aside Modal" 
    open
    isStickyTitle
    onClose={() => function('function to close the aside modal')}
/>
`}/>


<br />
<br />

##### <span name="floating-nav">API Reference</span>

<CustomTableDocs
    dataTable={[
        { title: 'titleVariant', default: 'h4', types: ['a', 'label', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'p', 'small', 'span', 'currency', 'anchorSmall'] },
        { title: 'position', default: 'right', types: ['right', 'left'] },
        { title: 'open', default: 'false', types: ['boolean'] },
        { title: 'disableEscapeKeyDown', default: 'false', types: ['boolean'] },
        { title: 'title', default: null, types: ['string'] },
        { title: 'onClose', default: null, types: ['function'] },
        { title: 'isStickyTitle', default: 'false', types: ['boolean'] },
        { title: 'children', default: null, types: ['ReactNode'] }
    ]}
/>

<AlertReactKnowledge />

<BackAndForwardController />