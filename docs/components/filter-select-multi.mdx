---
product: dd360-ds
title: Filter Select Multi component
components: Filter Select Multi
---

<br id='top' />
<br />

<Label>Components</Label>

# <HeaderDocCustom title="Filter Select Multi" url="https://main--62ffec7466615c40c8dbe435.chromatic.com/?path=/story/components-filterselectmulti--filter-select-multi" />


The FilterSelectMulti component allows users to select multiple options from a set of filters. Provides an interactive and easy-to-use interface to efficiently filter and organize data.

<br />
<br />
##### <span name="floating-nav">Imports</span>

<WindowEditor codeString="import { FilterSelectMulti } from 'dd360-ds'" />

<br />
##### <span name="floating-nav">Usage</span>

Use the FilterSelectMulti component as shown below:

<Playground className="gap-y-2">
 <FilterSelectCustom isMulti />
</Playground>

The code snippet below shows how to use the FilterSelectMulti component:

<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelectMulti
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B'
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />

##### <span name="floating-nav">Initial Item List</span>

The prop <Tag text='initialItemList' /> it is used to show the available options, there is also the possibility of disabling one of them
<Playground className="gap-y-2">
 <FilterSelectCustom disabled isMulti />
</Playground>


<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelectMulti
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B',
        disabled:true
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />

##### <span name="floating-nav">Position</span>

To manage the FilterSelectMulti component's visibility and position, you can use the useState hook in a functional component. Position stores the top and left coordinates, as well as a boolean indicating whether the component should be shown. The component listens for clicks on a button, which toggles the FilterSelectMulti visibility and updates its position based on the click event's coordinates.

<Playground>
  <FilterSelectCustom  isMulti/>
</Playground>

The code snippet below shows how to handle the visibility and position:

<WindowEditor
    codeString={`import React, { useEffect, useState } from 'react'
import { FilterSelectMulti, Flex, Text } from 'dd360-ds'
import { FilterIcon } from '@heroicons/react/outline'

const FilterSelectMultiCustom = () => {
 const [domLoaded, setDomLoaded] = useState<boolean>(false)
 const [position, setPosition] = useState<{
    top: number
    left: number
    show: boolean
  }>({
    top: 0,
    left: 0,
    show: false
  })

  useEffect(() => { setDomLoaded(true)}, [])

  return (
    <>
      {domLoaded && (
        <>
          <div className="h-8">
            <button  onClick={(event) => {
              setPosition((position) => ({
                top: event.pageY,
                left: event.pageX,
                show: !position.show
              }))
            >
              <Flex gap="3" justifyContent="center" alignItems="center">
                <Text className="mt-0.5 mr-1" bold>
                  FilterSelectMulti
                </Text>
                <FilterIcon className="w-4 h-4" />
              </Flex>
            </button>
          </div>
            <FilterSelectMulti
              position={position}
              initialItemList={{
                A: {
                  label: 'Option A'
                },
                B: {
                  label: 'Option B',
                  disabled
                }
              }}
              onApply={handleChange}
            />
        </>
      )}
    </>
  )
}

export default FilterSelectMultiCustom
 `}/>

Note: The "domLoaded" state variable in the FilterSelectMultiCustom is used to ensure that the component is fully rendered before displaying the FilterSelect Multi component. This is important when using Next.js, as it performs server-side rendering by default. By waiting for the client-side rendering to complete, we can avoid any issues with the FilterSelectMulti component not being fully initialized.

<br/>

##### <span name="floating-nav">Title</span>

The prop <Tag text='title' /> serves to provide a descriptive title or label

<Playground className="gap-y-2">
 <FilterSelectCustom isMulti title="Title Example" />
</Playground>


<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelectMulti
    title="Title Example"
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B'
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />

<br />

##### <span name="floating-nav">Text Apply Btn</span>

The prop <Tag text='textApplyBtn' /> it is used to customize the text of the apply button

<Playground className="gap-y-2">
 <FilterSelectCustom isMulti textApplyBtn="Apply Example"/>
</Playground>


<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelectMulti
    textApplyBtn="Apply Example"
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B'
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />

##### <span name="floating-nav">Text Reset Btn</span>

The prop <Tag text='textResetBtn' /> it is used to customize the text of the reset button

<Playground className="gap-y-2">
 <FilterSelectCustom isMulti textResetBtn="Reset Example"/>
</Playground>


<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelect
    textResetBtn="Reset Example"
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B'
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />

##### <span name="floating-nav">Width</span>

The prop <Tag text='width' /> used to customize the size

<Playground className="gap-y-2">
 <FilterSelectCustom isMulti width={250}/>
</Playground>


<WindowEditor
    codeString={`import { FilterSelectMulti } from 'dd360-ds'
  
<FilterSelect
    width={250}
    position={{
      top: 0,
      left: 0,
      show: true
    }}
    initialItemList={{
      A: {
        label: 'Option A'
      },
      B: {
        label: 'Option B'
      }
    }}
    onApply={handleChange}
/>
`}
/>

<br />
<br />

##### <span name="floating-nav">API Reference</span>

<CustomTableDocs
    dataTable={[
        { title: 'title', types: ['string'] },
		    { title: 'initialItemList', types: ['Object'] ,required:true},
        { title: 'textApplyBtn',default:'Apply', types: ['string'] },
        { title: 'textResetBtn',default:'Reser', types: ['string'] },
        {
         title: 'position',
         default: '{ show: false, top: 0, left: 0 }',
         types: ['{ show: boolean, top: number, left: number }']
        },
        { title: 'className', types: ['string'] },
        { title: 'width', types: ['Object'] },
	    { title: 'onApply', default: 'null', types: ['function'] ,required:true}
    ]}

/>

Note: This documentation assumes that the audience has basic knowledge of React and how to use components in React applications.

<BackAndForwardController />